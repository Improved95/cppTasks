#include <iostream>
#include "Converter.h"

size_t NsuConverterI::secondNumber = 0;
int NsuMute::convert() {
//    char *samplesInSecond = this->inputsVector[this->inputStreamInfo.first]->getSamplesInOneSecond();
//
//    if (secondNumber >= this->inputStreamInfo.second.first && secondNumber <= this->inputStreamInfo.second.second) {
//
//    }

    return 0;
}

int NsuMix::convert() {
    return 0;
}
